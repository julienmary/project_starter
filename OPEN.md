---
title: Open
status: open
priority: 1
depends_on: []
---

# OPEN

What is **not decided**. Better than fake consensus.

An oracle-vault (everything looks settled while you only explored) is a method failure. If it is still fuzzy, it lives here.

Format:

```
- [ ] Topic — why it is open, what would let you decide. Linked node.
```

## Pending

- [ ] Technical stack — no choice until [[nodes/purpose]] is frozen.
- [ ] Domain node split — born from compilation, not from the template.
- [ ] 2026-09-11 (convergence, uncovered) — The hook refuses a commit while a tracked file matches `.gitignore`. Real, consequential behavior, covered only by README and the hook's header — no decision, no node. Log it or fold it into one.
- [ ] 2026-09-11 (convergence, hole) — [[CONVERGE]] routes `missing`/`partial` gaps to "the node concerned", but a promise can trace to a stable root page ([[AGENTS]], [[COMPILE]], [[CONVERGE]]) that is not a node. Decide where those gaps live.
